{
  "name": "student-information-system-v4-cloudflare",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers + Hono + Turso Student Information Management System",
  "scripts": {
    "build": "node scripts/copy-flaticon.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "check": "node --check src/index.js && node --check src/db.js && node --check src/security.js && node --check src/ui.js"
  },
  "dependencies": {
    "@flaticon/flaticon-uicons": "latest",
    "@tursodatabase/serverless": "latest",
    "hono": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
