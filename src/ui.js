export const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;");
export const fmt=v=>v?new Date(v).toLocaleString():"—";export const dateOnly=v=>v?new Date(v).toLocaleDateString():"—";
const icon=n=>`<i class="fi fi-rr-${n}"></i>`;const credit=()=>`<a href="https://www.flaticon.com/uicons" target="_blank" rel="noreferrer">UIcons by Flaticon</a>`;
export function authPage({title,org,content}){return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · ${esc(org?.short_name||"Student IMS")}</title><link rel="stylesheet" href="https://cdn-uicons.flaticon.com/3.0.0/uicons-regular-rounded/css/uicons-regular-rounded.css"><link rel="stylesheet" href="/app.css"></head><body><main class="auth-shell"><section class="auth-card">${content}<div class="credit">${credit()}</div></section><aside class="auth-side"><div>${icon("shield-check")}<h2>Simple access.<br>Strong control.</h2><p>Manage students and staff permissions from one clean, secure workspace.</p><ul><li>${icon("check")} Role-based access</li><li>${icon("check")} Granular permissions</li><li>${icon("check")} Activity tracking</li></ul></div></aside></main><script src="/app.js"></script></body></html>`}
const nav=(path,current,label,ico)=>`<a class="${current===path||(path!=="/dashboard"&&current.startsWith(path))?"active":""}" href="${path}">${icon(ico)}<span>${esc(label)}</span></a>`;
export function layout({title,org,user,currentPath,can,csrf,body}){const links=[can("dashboard.view")?nav("/dashboard",currentPath,"Dashboard","home"):"",can("students.view")?nav("/students",currentPath,"Students","student"):"",can("batches.view")?nav("/academic/batches",currentPath,"Batches","calendar"):"",can("lecturers.view")?nav("/academic/lecturers",currentPath,"Lecturers","chalkboard-user"):"",can("subjects.view")?nav("/academic/subjects",currentPath,"Subjects","book-open-cover"):"",can("groups.view")?nav("/academic/groups",currentPath,"Groups","users-alt"):"",can("reports.view")?nav("/reports",currentPath,"Reports","chart-histogram"):"",can("users.view")?nav("/users",currentPath,"Users & Permissions","user-gear"):"",can("activity_logs.view")?nav("/activity-logs",currentPath,"Activity Logs","time-past"):"",can("settings.view")?nav("/settings",currentPath,"Settings","settings"):""].join("");const logo=org?.logo_url?`<img src="${esc(org.logo_url)}" class="brand-logo">`:`<div class="brand-logo brand-fallback">${icon("graduation-cap")}</div>`;return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · ${esc(org?.short_name||"Student IMS")}</title><link rel="stylesheet" href="https://cdn-uicons.flaticon.com/3.0.0/uicons-regular-rounded/css/uicons-regular-rounded.css"><link rel="stylesheet" href="/app.css"></head><body><div class="app"><aside class="sidebar" id="sidebar"><div class="brand">${logo}<div><strong>${esc(org?.short_name||"Student IMS")}</strong><span>Management System</span></div></div><nav>${links}</nav><div class="side-user"><span class="avatar">${esc(user.full_name[0]?.toUpperCase())}</span><div><strong>${esc(user.full_name)}</strong><span>${esc(user.role.replaceAll("_"," "))}</span></div></div></aside><section class="workspace"><header class="topbar"><button class="icon-btn mobile" data-sidebar>${icon("menu-burger")}</button><div class="spacer"></div><div class="account-wrap"><button class="account-btn" data-account><span class="avatar small">${esc(user.full_name[0]?.toUpperCase())}</span><span><strong>${esc(user.full_name)}</strong><small>${esc(user.role.replaceAll("_"," "))}</small></span>${icon("angle-small-down")}</button><div class="dropdown" data-account-menu><a href="/profile">${icon("user")} My Profile</a>${can("settings.view")?`<a href="/settings">${icon("settings")} Settings</a>`:""}<form method="post" action="/logout"><input type="hidden" name="_csrf" value="${esc(csrf)}"><button>${icon("sign-out-alt")} Logout</button></form></div></div></header><main class="page">${body}</main><footer><span>${esc(org?.short_name||"Student IMS")}</span>${credit()}</footer></section></div><div class="modal" data-modal hidden><div class="modal-card">${icon("triangle-warning")}<h3 data-modal-title>Confirm action</h3><p data-modal-text>Are you sure?</p><div><button class="btn ghost" data-cancel>Cancel</button><button class="btn danger" data-ok>Confirm</button></div></div></div><script src="/app.js"></script></body></html>`}
export function alert(type,text){return text?`<div class="alert ${type}">${icon(type==="success"?"check-circle":"circle-xmark")} ${esc(text)}</div>`:""}
export function pageHeader(k,t,d,a=""){return `<div class="page-head"><div><span class="kicker">${esc(k)}</span><h1>${esc(t)}</h1><p>${esc(d)}</p></div><div class="actions">${a}</div></div>`}
export const statusPill=s=>`<span class="pill ${s==="ACTIVE"?"green":"gray"}">${esc(s)}</span>`;export const rolePill=r=>`<span class="pill ${esc(r.toLowerCase())}">${esc(r.replaceAll("_"," "))}</span>`;export const emptyState=(i,t,x="")=>`<div class="empty">${icon(i)}<h3>${esc(t)}</h3>${x?`<p>${esc(x)}</p>`:""}</div>`;
